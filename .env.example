# CoinGecko Pro API key for real benchmark data (BTC/HYPE prices)
# Without this, the benchmark section is hidden on vault detail pages
COINGECKO_API_KEY=

# Anthropic API key for AI features (strategy classification, DD memos, chat)
# Without this, the AI analysis section is hidden on vault detail pages
ANTHROPIC_API_KEY=
